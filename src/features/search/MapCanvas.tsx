/**
 * Removed.
 *
 * This drew a schematic neighbourhood — invented streets in roughly the right
 * arrangement — because react-native-maps needs a billed Google Maps key and
 * has no web implementation at all. It was honest about being approximate and
 * no use for the one thing a map is here to do, which is get someone to a
 * doorway. StreetMap (OpenStreetMap tiles, no key) replaced it.
 *
 * The file is kept as a stub rather than deleted so that a stale copy on a
 * machine that has not pulled cannot quietly resurrect the old map.
 */
export {};
